import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact Us - Vedu APK Downloads",
  description: "Get in touch with the Vedu APK Downloads team for any questions, support, or feedback.",
};

export default function ContactPage() {
  return <ContactClient />;
}